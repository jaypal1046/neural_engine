Question: What is a REST API?

Ideal Answer: REST (Representational State Transfer) is an architectural style for web APIs using HTTP methods to perform actions on resources.

**HTTP verbs:**
- `GET /users` — retrieve list of users
- `POST /users` — create a new user
- `PUT /users/42` — replace user 42
- `PATCH /users/42` — partially update user 42
- `DELETE /users/42` — delete user 42

**Key principles:**
- **Stateless** — each request contains all info needed (no server sessions)
- **Resource-based** — URLs represent nouns, not actions (`/users`, not `/getUsers`)
- **JSON responses** — standard data format

**Status codes:** 200 OK, 201 Created, 404 Not Found, 500 Server Error.

Skills demonstrated: HTTP verbs, URL examples, three principles, status codes